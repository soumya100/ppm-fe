import { FC } from 'react'

interface AccountLedgerFormProps {
  
}

const AccountLedgerForm: FC<AccountLedgerFormProps> = ({}) => {
  return <div>AccountLedgerForm</div>
}

export default AccountLedgerForm